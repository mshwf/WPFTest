<Window x:Class="WPFTest.Wnd_Random"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFTest"
        mc:Ignorable="d"
        Title="Wnd_Random" Height="500" Width="500">
  
    <Grid>
        <!--<ListBox Name="lstBx" ItemsSource="{Binding Movies}" DisplayMemberPath="Title" VerticalAlignment="Top"/>
        <TextBlock Text="{Binding ElementName=lstBx, Path=SelectedItem.Title}" VerticalAlignment="Center"/>-->
        <Rectangle Width="100" Height="100" Fill="#ccffd700">
            <!--<Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Offset="0" Color="Gray"/>
                    <GradientStop Offset="0.4" Color="Black"/>
                    <GradientStop Offset="0.5" Color="Gray"/>
                    <GradientStop Offset="0.6" Color="Black"/>
                    <GradientStop Offset="1" Color="Gray"/>
                </LinearGradientBrush>
            </Rectangle.Fill>-->
        </Rectangle>
    </Grid>
</Window>
